Breadth-First Search (BFS) is an algorithm that explores a graph level by level.
It ensures that all nodes at the current depth are visited before moving to the next.
It is widely used in shortest path problems and AI search applications. Testing manual update.